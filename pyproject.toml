[project]
name = "private-note"
version = "0.1.0"
description = "Local RAG note system"
requires-python = ">=3.10"
dependencies = [
    "chromadb>=0.4",
    "psutil>=6.0",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.19.0",
]
